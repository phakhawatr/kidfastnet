

# ปรับปรุง UI Icons และ Contrast ให้มืออาชีพ

## ปัญหาที่พบ

1. **Icons ซ่อนอยู่** -- ปุ่ม Hint (💡) และ สมการ (f(x)) ใช้ `opacity-0` ซ่อนไว้ จะเห็นก็ต่อเมื่อ hover เท่านั้น ทำให้ผู้ใช้ไม่รู้ว่ามีปุ่มอยู่ และใช้ไม่ได้บนมือถือ/แท็บเล็ต
2. **สีไม่ชัดเจน** -- icon ใช้ `text-slate-400` บนพื้น `slate-800` ทำให้คอนทราสต์ต่ำ

## การแก้ไข

| ไฟล์ | การเปลี่ยนแปลง |
|------|----------------|
| `src/pages/MixedMathPracticeApp.tsx` | แก้ไข UI ของ question card (บรรทัด 557-559) |

### รายละเอียด

1. **ลบ `opacity-0 group-hover:opacity-100`** -- ให้ icons แสดงตลอดเวลา
2. **เพิ่มสีที่ชัดเจน** -- เปลี่ยน icon จาก `text-slate-400` เป็นสีที่มี contrast สูงขึ้น:
   - Hint (💡): `text-amber-400/70` พร้อม `hover:text-amber-300` และ `bg-amber-500/10`
   - Equation (f(x)): `text-indigo-400/70` พร้อม `hover:text-indigo-300` และ `bg-indigo-500/10`
3. **เพิ่ม background ให้ปุ่ม** -- ใส่ `bg-slate-700/50 rounded-lg` เป็น default เพื่อให้เห็นว่าเป็นปุ่มกดได้

ผลลัพธ์: Icons จะมองเห็นชัดเจนทั้งบน desktop และ mobile โดยไม่ต้อง hover

